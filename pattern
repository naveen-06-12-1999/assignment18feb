#include <stdio.h>

int main()
{
    int i,j,k=5;
    for(i=1;i<=k;i++){
        for(j=1;j<=k;j++){
            if(i<=j)
        printf("*");
        else
        printf("-");
    }
    printf("\n");
    }

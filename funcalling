#include <stdio.h>
int naveen(int d[],int s)
{
    for(int i=0;i<s;i++)
    {
        printf("%d",d[i]);
    }
}
int main()
{
    int a[]={1,2,3,4,5,6};
    int s=sizeof(a) /sizeof(a[0]);
    naveen(a,s);
    return 0;
}
